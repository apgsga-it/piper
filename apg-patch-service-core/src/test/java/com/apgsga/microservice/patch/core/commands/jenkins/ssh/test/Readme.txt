JHE - 01.09.2020
- All tests within this package are disabled by default.
- They were written in order to quickly test ssh and curl commands
- However, they all require a configured Jenkins server with jobs matching job's names define within tests ... therefore, it makes no sense to keep them active by default.